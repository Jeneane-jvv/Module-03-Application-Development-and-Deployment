NODE_ENV=development
PORT=5000

PGHOST=localhost
PGPORT=5432
PGDATABASE=firstcommit_mission_control
PGUSER=firstcommit_app
PGPASSWORD=replace_with_local_password
DATABASE_TLS_MODE=disable

JWT_SECRET=replace_with_secure_secret
JWT_EXPIRES_IN=1h

# Fictional demonstration account passwords used by the seed script.
# Replace these locally. Never commit real passwords.
SEED_LEARNER_PASSWORD=change_me
SEED_REVIEWER_PASSWORD=change_me
